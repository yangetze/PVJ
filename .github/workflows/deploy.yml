# DEPRECATED — deployment is now handled manually via Hostinger.
# This workflow is disabled (no triggers). To re-enable, restore the `on:` block.
#
# Previous approach: GitHub Actions called deploy.php via HTTPS webhook,
# which ran git pull on the server. Replaced by direct Hostinger deployment.

on: []
